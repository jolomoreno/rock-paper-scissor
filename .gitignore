# Godot 4+ specific ignores
.godot/
.import/
export.cfg
# export_presets.cfg is tracked on purpose: GitHub Actions needs it to know
# what to export. It has no secrets (Web export, no signing keys).

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Exported builds
build/
export/

# Local-only design documentation (not for the public repo)
0-documentation/

# macOS
.DS_Store

# Claude Code local settings
.claude/
